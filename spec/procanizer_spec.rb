RSpec.describe Procanizer do
  context "#add_proc_for" do
    it "creats proc for given method" do
    end

    context "called" do
      context "before method" do
      end

      context "with method definition as argument" do
      end

      context "after method" do
      end
    end

    context "aliased as with_proc" do
    end

    context "keeps method privacy level" do
      context "public" do
      end
    
      context "private" do
      end
    
      context "protected" do
      end
    end
  end

  it "has a version number" do
    expect(Procanizer::VERSION).not_to be nil
  end
end